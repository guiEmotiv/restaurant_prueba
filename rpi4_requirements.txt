# Requirements para RPi4 Print Queue Worker
# Instalar con: pip3 install -r rpi4_requirements.txt

requests>=2.28.0
pyusb>=1.2.1

# Opcional para mejor logging
colorlog>=6.7.0

# Para systemd service (opcional)
python-systemd>=234