# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DEVELOPMENT OVERRIDES - Simple and reliable
# Usage: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d app
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

services:
  app:
    environment:
      - DEBUG=True
      - DJANGO_SETTINGS_MODULE=backend.settings
      - DATABASE_NAME=restaurant_dev.sqlite3
      - USE_COGNITO_AUTH=True
      - ALLOWED_HOSTS=*
      - AWS_REGION=us-west-2
      - COGNITO_USER_POOL_ID=us-west-2_bdCwF60ZI
      - COGNITO_APP_CLIENT_ID=4i9hrd7srgbqbtun09p43ncfn0
    volumes:
      - ./data:/app/data
      - ./backend:/app/backend  # Hot-reload for development
    ports:
      - "8000:8000"